# Resume Engine Evaluation Archive

Internal evaluation evidence for `$recruiter` resume behavior. This module is not a production builder and not a standalone skill.

Production resume generation routes through `../brandedresume/GUIDE.md` and its repository-owned builder. Historical evaluation-run archives are intentionally excluded from this distributable authority package. They do not authorize placeholders in client-facing documents or external Drive writes.
